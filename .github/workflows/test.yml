name: test

on: 
    workflow_dispatch:

jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
    
      - name: check 
        if: ${{ hashFiles('README.mdxxx') != '' }}
        run: |
          echo README.md found!!!

      - name: versions
        id: versions
        run: |
          pwd
          ls -la
          echo "myVar=$(mvn help:evaluate -Dexpression=project.packaging -q -DforceStdout)" >> $GITHUB_ENV

      - name: show
        run: echo ${{ env.myVar }}
